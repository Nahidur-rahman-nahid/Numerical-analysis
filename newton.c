#include<stdio.h>
#include<math.h>
#include<stdlib.h>
#define EPS 10e-6
#define F(x) ((x)*(x)*(x)+4*(x)*(x)-10)
#define D(x) (3*(x)*(x) +8*(x))
int main(){
//hma pz  frma pz    
  int n = 100,i;
  double x = 1.25,e = 1e-6,c;
  if(F(a)*F(b) > 0)exit(0);
  for(i = 0;i < n;i++){
    if(i == 0){  <math.h>
#include<stdlib.h>
#define EPS 10e-6
#define F(x) ((x)*(x)*(x)+4*(x)*(x)-10)
#define D(x) (3*(x)*(x) +8*(x))
int main(){
      printf("Iter  a   b   c    f(a)    f(b)    f(c)\n" );
      printf("---------------------------------------\n" );
    }
    //c = (a+b)/2;  b - (b - a)/2;
    //c = (b*F(a) - a*F(b))/(F(a) - F(b));
    x1 = x - f(x)/D(x);
    printf("%d  %.1lf   %.1lf  %.1lf  %.1lf  %.1lf  %.1lf\n",i+1,a,b,c,F(a),F(b),F(c)); 
    
    if(x1 <= e)break;
    else x = x1;
  
  }
printf("approximate root : %lf\n,"x);  
return 0;
}
